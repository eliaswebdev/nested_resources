<p id="notice"><%= notice %></p>
<table class="table">
  <tbody>
    <tr>
      <th>Make:</th>
      <td><%= @model.make_id %></td>
    </tr>

    <tr>
      <th>Name:</th>
      <td><%= @model.name %></td>
    </tr>
  </tbody>
</table>
<div class="text-center">
  <%= link_to 'Edit', edit_make_model_path(@make, @model), class: 'btn btn-primary' %>
  <%= link_to 'Back', make_models_path(@make), class: 'btn btn-secondary' %>
</div>
